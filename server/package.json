{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "nodemon --ignore build/ -e ts,json --exec \"rm -rf build/*; cp -r src/public build/; tsc && tsc-alias --silent && node build/app.js\""
  },
  "dependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^17.0.8",
    "express": "^4.17.2",
    "nodemon": "^2.0.15",
    "typescript": "^4.5.4"
  },
  "devDependencies": {
    "tsc-alias": "^1.5.0"
  }
}
